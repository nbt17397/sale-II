import { enUS } from "@mui/x-data-grid"

const en = {
    switchToLightTheme: 'Switch to light theme',
    switchToDarkTheme: 'Switch to dark theme',
    loading: 'Loading',
    findSomethingWithValue: (something) => `Find ${something} with value`,
    or: 'or',
    datagrid: enUS.components.MuiDataGrid.defaultProps.localeText,
}
export default en